# AGENTS.md

这个仓库是一个长期演进的个人知识库，可以包含论文调研、前端、后端、AI、工程实践、工具链、读书笔记、项目复盘等不同主题。根目录的 `AGENTS.md` 只规定全局维护方式；具体专题怎么组织，应由专题目录内的 `README.md` 或局部 `AGENTS.md` 说明。

## 核心原则

- 先理解已有内容，再新增或调整。开始任务前先查看目录结构和相关文件，避免重复建设。
- 知识库优先使用 Markdown、JSON、CSV 等可检索、可 diff、可长期维护的文本格式。
- 根目录保持克制，只放全局说明、入口索引、通用模板和跨专题配置。
- 一级目录通常代表一个知识域、项目域或长期专题。新增一级目录前，应确认它不是已有专题的子主题。
- 专题目录应该自解释：重要目录最好有 `README.md`，复杂专题可以增加局部 `AGENTS.md`。
- 不要为了“整理”而静默移动、重命名或删除用户已有资料。结构性调整前先说明理由和影响范围。
- 对不确定的信息明确标注“不确定”“待核验”或“推测”，不要把推断写成事实。

## 组织方式

本仓库不强制所有专题使用同一套目录结构。不同类型知识可以有不同形态，例如论文追踪、技术笔记、项目复盘、代码片段、资源清单等。

推荐每个专题至少回答这些问题：

- 这个专题研究或积累什么？
- 当前有哪些入口文件或索引？
- 新资料应该放在哪里？
- 哪些内容已经整理过，哪些还只是临时收集？
- 后续待办、疑问和待核验信息在哪里记录？

常见但非强制的文件包括：

- `README.md`：专题入口、范围、维护方式。
- `index.md`：人工可读索引，连接关键笔记、总结和资源。
- `backlog.md`：待办、待查问题、未整理材料。
- `notes/`：普通知识笔记。
- `papers/` 或 `literature/`：论文、文献、资料追踪。
- `summaries/` 或 `surveys/`：阶段性总结、综述、复盘。
- `resources/`：链接、工具、数据源、参考材料。

只有在确实要写入内容时再创建目录，不要提前创建大量空目录。

## 信息生命周期

新增知识时，尽量让它经历从“收集”到“沉淀”的过程：

1. **Capture**：先记录来源、时间、主题和为什么值得保存。
2. **Triage**：判断它属于哪个专题，是否和已有内容重复。
3. **Organize**：放入合适目录，补充标题、标签、状态和链接。
4. **Synthesize**：定期把零散笔记整理成阶段性总结、方法对比或复盘。
5. **Review**：标记哪些结论已核验，哪些仍需要更新或二次确认。

不要把所有内容都写成一次性长文。更好的方式是保留可迭代的索引、笔记和总结，让未来可以继续补充。

## 命名与格式

- 新文件和新目录优先使用小写英文、数字和连字符，例如 `frontend-performance.md`。
- 已有目录名不强制改名；如果要统一命名，先确认没有外部引用或用户约定。
- 日期使用 `YYYY-MM-DD`。
- 中文正文可以保留中文；元数据字段建议稳定，例如 `title`、`date`、`source`、`status`、`tags`。
- 同一主题不要重复创建多个近似文件。新增前先搜索标题、关键词和同义词。
- 大文件、PDF、图片、数据集不要直接堆进根目录。是否纳入仓库、放在哪里、是否需要 Git LFS，应先和用户确认。

## 来源与核验

任何可能随时间变化的信息，都需要记录来源和核验日期。包括但不限于：

- 论文发表状态、DOI、期刊分区、引用情况、代码和数据集链接。
- 技术文档、API、框架版本、模型能力、工具用法。
- 榜单、价格、许可证、政策、法规、公司或项目状态。

如果当前无法联网核验，应明确写上“未联网核验”或“待核验”，不要把未确认内容包装成确定事实。需要引用外部内容时，优先使用短引用和自己的概括，并保留原始链接。

## 索引与交叉链接

- 每个长期专题应有一个稳定入口，通常是专题目录的 `README.md`。
- 阶段性总结应反向链接到关键资料，关键资料也应能被索引找到。
- 跨专题内容不要随意复制多份。优先在一个地方维护主笔记，再用链接指向它。
- 如果两个专题边界重叠，在入口文件中说明关系，而不是把材料混放。

## 自动化助手操作规则

- 开始任何任务前，先用 `rg --files`、`find` 或类似命令快速了解当前结构。
- 修改前先读相关文件；不要只凭文件名猜内容。
- 新增内容时，优先更新已有索引、入口或 backlog，避免制造孤立文件。
- 结构性调整、批量重命名、删除资料、移动大文件之前，必须先征得用户确认。
- 遇到重复、冲突或命名不一致时，先标注问题并提出合并策略，不要擅自丢弃信息。
- 修改多个文件后，说明改了哪些文件、为什么改、还有哪些后续建议。
- 不要提交 `.DS_Store`、临时缓存、构建产物或编辑器私有文件。

## 局部规则优先级

如果某个专题目录内存在自己的 `AGENTS.md`，处理该目录内容时应同时遵守：

1. 用户当前明确要求。
2. 更靠近目标文件的局部 `AGENTS.md`。
3. 根目录 `AGENTS.md`。
4. 已有文件和目录体现出的惯例。

当局部规则和根目录规则冲突时，优先遵守更具体、距离目标文件更近的规则；如果会造成破坏性影响，先询问用户。
